/* program to create a package specification */

create or replace package apec is
  procedure show_deptname(eno number);
  procedure show_emps(dno number);
  function get_salary(eno number) return number;
  function get_name(eno number) return varchar2;
end;
/
